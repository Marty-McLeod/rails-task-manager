<!-- Displays form populated with task.id values so user can edit the task -->
<div class="container">
    <% page_name = edit_task_path.split('/')[-1] %>
    <h1 class="my-3"><%= page_name.capitalize() %> task</h1>    
    <!-- render task fields form here -->
    <%= render "form", task: @task %>
</div>